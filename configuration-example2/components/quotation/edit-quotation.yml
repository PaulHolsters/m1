components:
  # todo | nakijken hoe je configureert dat er teruggegaan kan worden naar een vorige toestand
  # todo | alsook hoe er omgegaan moet worden met conflicterend options qua prijs en omschrijving
  # todo | en met conflicterende product eigenschappen en product namen
  - name: edit quotation
    type: form
    route: offertes/aanpassen/:ID
    configuration:
      - concept: quotation
      - action: create
        data:
          - fields:
              - field: groupId
      - fields:
          - field: selected product
            label: Product
            format:
              - name: show
                values:
                  - value: identifying name
          - field: VAT
            label: BTW
            validation: restrict
          - field: discount
            label: Korting
            validation: restrict
            # todo
            # de engine zal dit als een checkbox presenteren aan de frontend
            # omdat in de concepttypes er staat list: true
            # de bewaarde gegevens zullen daarbij gepresenteerd worden
            # deze kunnen verschillen met de waarde zoals die gekend is voor een optie in de DB
            # indien in de frontend niet voorzien is om de nieuwe naam te selecteren zal dit niet mogelijk zijn
            # als dev kan je dit oplossen door de nieuwe opties te callen en zelf een systeem te voorzien
            # ik ga dit dan ook zo laten => kan je opties verwijderen waar producten aan gekoppeld zijn? neen natuurlijk niet
            # zijn de frontedn componenten hier automatisch op voorzien ????
           # todo
          - field: selected options
            label: Opties
            format:
              - ref: name + price
          - field: selected extra services
            label: Extra te leveren diensten
            format:
              - ref: name + price
          - fieldset: customer info
            label: klantinfo
            fields:
              - field: firstname
                label: voornaam
              - field: lastname
                label: achternaam
              - fieldset: address
                label: adres
                fields:
                  - field: street
                    label: straat
                  - field: housenumber
                    label: huisnummer
                  - field: postalcode
                    label: postcode
                  - field: city
                    label: gemeente
              - field: e-mail

